'use strict';

angular.module('testApp.system').controller('EndController', ['$scope', 'Global', function ($scope, Global) {
    $scope.global = Global;

    // @TODO : if the test is not over redirect 


}]);
